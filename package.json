{
  "name": "project-agent-factory",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate": "node scripts/project-agents.mjs validate-factory",
    "validate:claude": "claude plugin validate . --strict && claude plugin validate .claude-plugin/plugin.json --strict"
  },
  "engines": {
    "node": ">=20"
  }
}
